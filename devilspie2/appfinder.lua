if (get_window_name() == "Application Finder") then
    set_window_position(500, 600)  -- X=500, Y=600 à ajuster selon ta résolution
    set_window_geometry(500, 600, 700, 500)  -- X, Y, largeur, hauteur
end
